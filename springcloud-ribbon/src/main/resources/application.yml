eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 8764
spring:
  application:
    name: service-ribbon

#暴露全部监控信息
management:
  endpoints:
    web:
      exposure:
        exclude: "*"

